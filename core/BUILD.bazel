load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "rtbot",
    srcs = glob([
        "src/**/*.cpp",
        "src/**/*.h",
    ]),
    includes = ["src"],
    deps = [
        "@yaml-cpp//:lib",
    ],
)
