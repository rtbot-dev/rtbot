load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "rtbot",
    srcs = ["test_join.cpp"],
    defines = ["CATCH_CONFIG_MAIN"],
    deps = [
        "//libs/core/lib:rtbot",
        "@catch2",
    ],
)
