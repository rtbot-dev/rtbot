load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "test",
    srcs = glob([
        "**/*.cpp",
        "**/*.h",
    ]),
    data = glob([
        "**/*.json",
        "**/*.csv",
    ]) + [
        "//examples/data:ppg.csv",
        "//examples/data:ppg.json",
    ],
    # defines = ["CATCH_CONFIG_MAIN"],
    deps = [        
        "//libs/core/api:rtbot-api",
        "@catch2",
    ],
)
