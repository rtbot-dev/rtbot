syntax = "proto3";

package rtbot.api.proto;

// TODO: re-enable protovalidate when protobuf version is updated
// import "buf/validate/validate.proto";

message Input {
    string id = 1;
    uint32 num_ports = 2;
}

message Output {
    string id = 1;
    uint32 num_ports = 2;
}
