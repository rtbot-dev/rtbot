#include "rtbot/compiled/jit/emit/Identity.h"

namespace rtbot::jit::emit {

// All logic is in the inline header. Source kept for symmetry with the rest
// of the emitter sources.

}  // namespace rtbot::jit::emit
